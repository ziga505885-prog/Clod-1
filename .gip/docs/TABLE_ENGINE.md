# DOCX Table Engine

Deterministic Word-table extraction and validation.

The column «Категория состояния» accepts only: аварийное, работоспособное, ограниченно-работоспособное, нормативное. Extra explanations are rejected.
